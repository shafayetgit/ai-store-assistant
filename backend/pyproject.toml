[project]
name = "backend"
version = "0.1.0"
description = "AIStoreAssistant is an AI-powered customer support and sales automation platform for online stores."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard]>=0.141.1",
]
